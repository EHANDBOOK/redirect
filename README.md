This repository contains HTML files with redirects to (potential) unstable links like newsletter or surveys.

Each redirect should reside in a separate folder with index.html file in it.
This allows to access the URL without <filename>.html
E.g. https://ehandbook.etas.com/redirect/newsletter/index.html -> https://ehandbook.etas.com/redirect/newsletter
